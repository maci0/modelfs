# Python tooling for this repo. The Zig code itself has no third-party
# dependencies; these cover scripts/check.sh (ruff, mypy). Benchmark figures
# are SVG written with the stdlib (scripts/run_benchmarks_and_plots.py).
# check.sh additionally requires the system tools zig (0.16) and shellcheck,
# which are not pip-installable and are intentionally not listed here.
# Install: uv venv .venv && uv pip install --require-hashes -r requirements-dev.lock.txt
#
# Exact pins matching requirements-dev.lock.txt. A range here would let
# `uv pip compile` float a newer ruff/mypy without a bounds-file diff.
# Bump a pin, then regenerate the lock (command in its header).
mypy==2.3.1
ruff==0.16.4
